numpy>=2.0,<3
opencv-python>=4.10,<5
Pillow>=11,<13
pytesseract>=0.3.13,<1
# Versao fixa: versoes mais recentes do RapidOCR passam a usar por defeito os
# modelos PP-OCRv6, que falham a leitura dos numeros da fita nestas imagens
# (a 3.8.1 usa os PP-OCRv4, que funcionam). Manter fixo para todos usarem o mesmo.
rapidocr==3.8.1
onnxruntime>=1.20,<2
